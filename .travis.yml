language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-backdroptheme' ]; then cd .. && eval "mv $currentfolder generator-backdroptheme" && cd generator-backdroptheme; fi